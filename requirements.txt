jiwer>=3.0.4
openai-whisper>=20231117
pandas>=2.0
pyyaml>=6.0
soundfile>=0.12
tabulate>=0.9
tqdm>=4.66

# Optional for Colab/GPU Hindi-tuned model comparison:
# pip install -e ".[colab]"

# Optional for compact LoRA fine-tuning:
# pip install -e ".[finetune]"
